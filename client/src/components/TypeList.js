import React from 'react'
import {Card, Image, Button, Icon, Grid} from 'semantic-ui-react';
import typeImage from '../utils/typeImage'

const TypeList = ({types, onTypeSelect}) => {
  return(
    <Grid>
      <Card.Group>
        {/*  */}
      </Card.Group>
    </Grid>
  )
}

export default TypeList